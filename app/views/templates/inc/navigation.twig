<nav class="navbar navbar-dark bg-success text-light navbar-expand-lg sticky-top">
  <a href="{{ path_for('pocetna') }}" class="navbar-brand text-uppercase"
    ><i class="fas fa-gem"></i> Exclusive Event Center</a
  >
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('pocetna') }}"><i class="fas fa-home"></i> Početna</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('valid') }}"><i class="fab fa-bootstrap"></i> Bootstrap4 validation test</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('ugovori') }}"><i class="fas fa-file-signature"></i> Ugovori</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('kalendar') }}"><i class="fas fa-calendar-alt"></i> Pregled</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('termin.pregled.get') }}"
          ><i class="fas fa-calendar-plus"></i> Zakazivanje</a
        >
      </li>
      {% if auth.logged and auth.user.nivo == 0 %}
      <li class="nav-item dropdown">
        <a
          class="nav-link dropdown-toggle"
          href="#"
          id="navbarDropdown"
          role="button"
          data-toggle="dropdown"
          aria-haspopup="true"
          aria-expanded="false"
        >
          <i class="fas fa-user-lock"></i> Administrator
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <a class="dropdown-item" href="{{ path_for('sale') }}">Sale</a>
          <a class="dropdown-item" href="{{ path_for('meni') }}">Meni</a>
          <a class="dropdown-item" href="{{ path_for('tip_dogadjaja') }}">Tipovi događaja</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{ path_for('admin.korisnik.lista') }}">Korisnici</a>
          <a class="dropdown-item" href="{{ path_for('logovi') }}">Logovi</a>
        </div>
      </li>
      {% endif %} {% if auth.logged %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('odjava') }}"><i class="fas fa-sign-out-alt"></i> Odjava</a>
      </li>
      {% else %}
      <li class="nav-item">
        <a class="nav-link" href="{{ path_for('prijava') }}"><i class="fas fa-sign-in-alt"></i> Prijava</a>
      </li>
      {% endif %}
    </ul>
  </div>
</nav>
